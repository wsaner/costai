<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.cost.boq.mapper.CostBoqImportErrorMapper">
    <resultMap id="ErrorResult" type="com.ruoyi.cost.boq.domain.CostBoqImportError">
        <id property="id" column="id"/>
        <result property="projectId" column="project_id"/>
        <result property="batchId" column="batch_id"/>
        <result property="sourceSheet" column="source_sheet"/>
        <result property="sourceRow" column="source_row"/>
        <result property="errorField" column="error_field"/>
        <result property="rawValue" column="raw_value"/>
        <result property="errorMessage" column="error_message"/>
        <result property="rawDataJson" column="raw_data_json"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectByBatchId" resultMap="ErrorResult">
        select id, project_id, batch_id, source_sheet, source_row, error_field, raw_value,
               error_message, raw_data_json, create_by, create_time
        from cost_boq_import_error
        where batch_id = #{batchId} and del_flag = '0'
        order by source_row asc, id asc
    </select>

    <insert id="batchInsert">
        insert into cost_boq_import_error (
            project_id, batch_id, source_sheet, source_row, error_field, raw_value,
            error_message, raw_data_json, create_by, create_time, del_flag
        ) values
        <foreach collection="errors" item="e" separator=",">
            (#{e.projectId}, #{e.batchId}, #{e.sourceSheet}, #{e.sourceRow}, #{e.errorField},
             #{e.rawValue}, #{e.errorMessage}, #{e.rawDataJson}, #{e.createBy}, #{e.createTime}, '0')
        </foreach>
    </insert>

    <update id="deleteByBatchId">
        update cost_boq_import_error set del_flag = '2', update_by = #{updateBy}, update_time = sysdate()
        where batch_id = #{batchId} and del_flag = '0'
    </update>
</mapper>
